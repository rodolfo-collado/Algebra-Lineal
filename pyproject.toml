[project]
name = "algebra-lineal"
version = "0.1.0"
description = "Proyecto educativo en Python para implementar manualmente algoritmos de matrices y álgebra lineal."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "colorama==0.4.6",
    "django>=6.1.1",
]

[tool.uv]
package = false
